import { ExecutionStatusStep } from './execution-status-step';

describe('ExecutionStatusStep', () => {
  it('should create an instance', () => {
    expect(new ExecutionStatusStep()).toBeTruthy();
  });
});
