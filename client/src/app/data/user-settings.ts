export class UserSettings {
    simple: boolean;
    events: boolean;

    conditionals: boolean;
    dances: boolean;
    loops: boolean;
    sensors: boolean;
    variables: boolean;

    isLoaded: boolean = false;
}
